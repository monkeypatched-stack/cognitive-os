# Reproducible-build lockfile for monkeybrain-runtime.
#
# SCOPE / HOW THIS WAS GENERATED:
# Pinned to the exact versions verified working in the dev sandbox this was
# generated in (originally 2026-07-23, extended 2026-09-09), for the subset
# of pyproject.toml's `dependencies` that are genuinely resolvable
# third-party PyPI packages. Kept in sync with requirements.txt (itself
# generated from pyproject.toml via scripts/sync_requirements.py) — same
# package set, this file just pins exact versions.
#
# NOT covered here — regenerate/extend on a real deployment host before relying
# on this as a complete lock:
#   1. Internal first-party packages (not on any public registry `uv`/`pip` can
#      resolve from this sandbox — `uv lock` fails on them with "not found in
#      the package registry"): monkeybrain-cerebellum, monkeybrain-broca,
#      monkeybrain-introspection, monkeybrain-cortex, monkeybrain-homeostasis,
#      monkeybrain-deepdive, monkeybrain-cingulate, monkeybrain-plasticity,
#      monkeybrain-sync, sittingface, soma-cli. Pin these from wherever your
#      private index / vendored source actually publishes them.
#
# lemon here is the PyPI package named `lemon` (note: that's lemon.markets, a
# stock-trading API client — unrelated to this repo's own
# src/introspection/lemon.py observability module; verify this dependency is
# even still needed before relying on the pin below).
#
# To regenerate/extend on a host with the full dependency set installed:
#   pip freeze | grep -iE '^(<new-package>)==' >> requirements-lock.txt
#
# Install with: pip install -r requirements-lock.txt

fastapi==0.137.1
uvicorn==0.49.0
motor==3.7.1
pydantic==2.13.4
pydantic-settings==2.14.1
pymongo==4.17.0
python-jose==3.5.0
redis==8.0.1
neo4j==6.2.0
nats-py==2.15.0
influxdb-client==1.50.0
paho-mqtt==2.1.0
python-multipart==0.0.32
bcrypt==5.0.0
boto3==1.43.42
httpx==0.28.1
PyYAML==6.0.3
aiohttp==3.14.1
numpy==2.4.6
scipy==1.17.1
typer==0.25.1
rich==15.0.0
lemon==1.3.1
watchfiles==1.2.0
networkx==3.6.1
phart==2.0.6
